#include <stdio.h>
#include <stdlib.h>

int main(){
    int nubers []={12,75,150,180,145,525,50};
    int size = sizeof (numbers)/sizeof (numbers [0]);
    for (int i =0; i < size ; i ++) {
    if (numbers[i] > 100&& numbers [i] <200) {
       printf ("**%d**\n", numbers [i]);
    }

    }
   return 0;

}
